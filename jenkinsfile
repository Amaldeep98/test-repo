pipeline {
    agent {label 'local-agent'}

    environment {
	    IMAGE_NAME = 'ayoobki/maven-app' 
    }

    stages {
        stage('Checkout') {
            steps {
                git url: https://github.com/Amaldeep98/Memory-Monster.git', branch: 'master'
            }
        }

        stage('Build') {
            steps {
                sh 'mvn clean package'
            }
        }

    post {
        success {
            mail to: 'ayoobkibrahim109@gmail.com',
                subject: "✅ SUCCESS: ${env.JOB_NAME} #${env.BUILD_NUMBER}",
                body: """Good news! The pipeline succeeded.
             
                • Job: ${env.JOB_NAME}
                • Build Number: ${env.BUILD_NUMBER}
                • URL: ${env.BUILD_URL}
                """
        }
        failure {
            mail to: 'ayoobkibrahim109@gmail.com',
                subject: "❌ FAILURE: ${env.JOB_NAME} #${env.BUILD_NUMBER}",
                body: """The pipeline failed.
             
                • Job: ${env.JOB_NAME}
                • Build URL: ${env.BUILD_URL}
                • Check console logs for error details.
                """
        }
        aborted {
            mail to: 'ayoobkibrahim109@gmail.com',
                subject: "🚫 ABORTED: ${env.JOB_NAME} #${env.BUILD_NUMBER}",
                body: """The build was manually aborted.
             
                • Job: ${env.JOB_NAME}
                • URL: ${env.BUILD_URL}
                • Check if it was canceled intentionally.
                """
        }
    }
}
